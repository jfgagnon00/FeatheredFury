from setuptools import find_packages, setup

setup(name="a57-tp-ci",
      packages=find_packages(),
      version="0.0.1",
      description="Détection espèce d'oiseau à partir d'une capture audio.",
      author="Jean-Francois Gagnon")
