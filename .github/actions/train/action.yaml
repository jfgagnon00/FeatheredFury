name: "Train"
description: "Genere modele"

inputs:
  NEPTUNE_API_TOKEN:
    description: "Token API Neptune"
    required: true

  BUILD_MACHINE:
    required: false
    default: None

runs:
  using: "composite"
  steps:
    - name: Indexer Donnees
      shell: bash
      run: |
        ffury dataset index

    - name: Entrainement
      env:
        NEPTUNE_API_TOKEN: ${{ inputs.NEPTUNE_API_TOKEN }}
        BUILD_MACHINE: ${{ inputs.BUILD_MACHINE }}
      shell: bash
      run: |
        ffury train
